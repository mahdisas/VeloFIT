import { ListPageSkeleton } from "@/components/skeletons";

/** Closest boundary for Finance sub-sections (categories / products / packages). */
export default function Loading() {
  return <ListPageSkeleton />;
}
